;;; 2-web-mode.conf.el --- summary -*- lexical-binding: t -*-

;; Author: ken
;; Maintainer: ken
;; Version: version
;; Package-Requires: (dependencies)
;; Homepage: homepage
;; Keywords: keywords

;;; Commentary:

;; commentary

;;; Code:

;; (defun personal-web-mode-hooks ()
;;   "My hooks for Web mode."
;;   (setq web-mode-markup-indent-offset 2))
;; (add-hook 'web-mode-hook 'personal-web-mode-hooks)

(setq web-mode-markup-indent-offset 2)
(provide '2-web-mode.conf)


;;; web-mode.conf.el ends here

